{
  "name": "kubi-api",
  "version": "1.0.0",
  "description": "Kubi API - Public HTTP API layer",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "lint": "bunx biome lint src/",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "bun-types": "latest",
    "typescript": "^5.7.0"
  }
}
